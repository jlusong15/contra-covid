'use strict';

describe('myApp.contactTracing module', function () {

  beforeEach(module('myApp.contactTracing'));

  describe('contactTracing controller', function () {

    it('should ....', inject(function ($controller) {
      //spec body
      var viewCtrl = $controller('ContactTracingCtrl');
      expect(viewCtrl).toBeDefined();
    }));

  });
});
